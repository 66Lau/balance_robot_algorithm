m = 10;
M = 1;
l = 0.8;
g = 9.8;

A = [0, 0,             1, 0;
     0, 0,             0, 1;
     0, m*g/M,         0, 0;
     0, (m+M)*g/(M*l), 0, 0]

B = [0;0;1/M;1/(M*l)]

Q = diag([0.001 0.001 3 0.001]); % Q是对于理想状态的权重 x q dx dq

R = 0.08;% R是对于控制变量的最小化

K = lqr(A,B,Q,R)